# Peugeot Vehicle OS v31 — Android Startup Reset
Android Studio project. Android boot -> Vehicle OS -> Display Ready -> 1.5s -> app restart once. Persistent flag prevents restart loop. Does not reboot Android hardware, access ECU, send CAN frames, or control actuators.
